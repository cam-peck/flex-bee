const answers = new Map();
answers.set(1, { levelDescription: 'text', levelAnswer: 'justify-content: flex-end' });
answers.set(2, { levelDescription: 'texting', levelAnswer: 'justify-content: center' });

export default answers;
